---
title: Chiffrement RSA Interactif
description: Outil de chiffrement et déchiffrement RSA
---

import RSACrypto from './RSACrypto'

# 🔐 Chiffrement RSA

Cet outil utilise l'API Web Crypto pour générer des clés RSA et chiffrer/déchiffrer des messages de manière sécurisée directement dans votre navigateur.

<RSACrypto />

## Comment ça marche ?

1. **Générer des clés** : Cliquez sur le bouton pour créer une paire de clés RSA (2048 bits)
2. **Chiffrer** : Entrez votre message et cliquez sur "Chiffrer" (utilise la clé publique)
3. **Déchiffrer** : Collez le message chiffré et cliquez sur "Déchiffrer" (utilise la clé privée)

## Sécurité

- Les clés sont générées localement dans votre navigateur
- Aucune donnée n'est envoyée à un serveur
- Utilise l'algorithme RSA-OAEP avec SHA-256
